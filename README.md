# commit-tracker
A simple python script using git that allows you to track changes to a repository.

To run all you have to do is have python and git installed.

open terminal and navigate to the assignment folder 

do python tracker.py

enter the path to the local repository

push enter

